// AOS.js
  AOS.init({
    delay: 200, // Values from 0 to 3000, in steps of 50ms
    duration: 1500, // Values from 0 to 3000, in steps of 50ms
    once: false, // whether animation should happen once while scrolling down
    mirror: false, // whether elements should animate out while scrolling past them
  });